# Embedding provider (OpenAI-compatible, e.g. LiteLLM proxy)
EMBEDDING_API_BASE=https://<your-litellm-host>/v1
EMBEDDING_API_KEY=sk-...
EMBEDDING_MODEL=nomic-embed-text

# Alternatively configure via: oc config set EMBEDDING_API_BASE <url>
